import Web3 from 'web3';

class Web3Util {
    static loadWeb3(){
        /*
        if(window.web3 !== undefined){
            return new Web3(window.web3.currentProvider);
        }
        */
    }
}